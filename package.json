{
  "name": "acme",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write .",
    "db:push": "bun --filter db push",
    "db:studio": "bun --filter db studio"
  },
  "devDependencies": {
    "@types/bun": "^1.1.1",
    "prettier": "^3.2.5",
    "turbo": "latest"
  },
  "workspaces": [
    "sites/*",
    "packages/*"
  ]
}